security:
  jwt:
    secret: "8650334ckf"
    expiration: 3600000 # 1 hour in ms
    refresh-expiration: 604800000 # 7 days in ms

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/qlm
    username: qlm
    password: qlmpass
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080

logging:
  level:
    root: INFO
